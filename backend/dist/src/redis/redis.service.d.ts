import { OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import Redis from 'ioredis';
export declare class RedisService implements OnModuleInit, OnModuleDestroy {
    private readonly logger;
    private client;
    private subscriber;
    constructor();
    onModuleInit(): Promise<void>;
    onModuleDestroy(): Promise<void>;
    getClient(): Redis;
    getSubscriber(): Redis;
    set(key: string, value: string | number | object, ttlSeconds?: number): Promise<void>;
    get<T = string>(key: string): Promise<T | null>;
    del(key: string): Promise<number>;
    delMultiple(keys: string[]): Promise<number>;
    exists(key: string): Promise<boolean>;
    expire(key: string, seconds: number): Promise<boolean>;
    ttl(key: string): Promise<number>;
    hset(key: string, field: string, value: string | number | object): Promise<number>;
    hsetMultiple(key: string, data: Record<string, string | number | object>): Promise<number>;
    hget<T = string>(key: string, field: string): Promise<T | null>;
    hgetall<T = Record<string, string>>(key: string): Promise<T | null>;
    hdel(key: string, field: string): Promise<number>;
    lpush(key: string, ...values: (string | number | object)[]): Promise<number>;
    rpush(key: string, ...values: (string | number | object)[]): Promise<number>;
    lpop<T = string>(key: string): Promise<T | null>;
    rpop<T = string>(key: string): Promise<T | null>;
    lrange<T = string>(key: string, start: number, stop: number): Promise<T[]>;
    llen(key: string): Promise<number>;
    sadd(key: string, ...members: (string | number)[]): Promise<number>;
    sismember(key: string, member: string | number): Promise<boolean>;
    smembers(key: string): Promise<string[]>;
    srem(key: string, ...members: (string | number)[]): Promise<number>;
    zadd(key: string, score: number, member: string | number): Promise<number>;
    zrange(key: string, start: number, stop: number, withScores?: boolean): Promise<string[]>;
    zrevrange(key: string, start: number, stop: number, withScores?: boolean): Promise<string[]>;
    zscore(key: string, member: string | number): Promise<number | null>;
    publish(channel: string, message: string | object): Promise<number>;
    subscribe(channel: string, callback: (message: string) => void): Promise<void>;
    unsubscribe(channel: string): Promise<void>;
    keys(pattern: string): Promise<string[]>;
    incr(key: string): Promise<number>;
    incrby(key: string, increment: number): Promise<number>;
    decr(key: string): Promise<number>;
    decrby(key: string, decrement: number): Promise<number>;
}
